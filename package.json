{
  "name": "chessdex",
  "private": true,
  "version": "0.1.0",
  "description": "ChessDex — Cross-platform chess game manager",
  "scripts": {
    "dev:desktop": "pnpm --filter @chessdex/desktop tauri dev",
    "dev:mobile": "pnpm --filter @chessdex/mobile start",
    "build:shared": "pnpm --filter @chessdex/shared build",
    "build:desktop": "pnpm --filter @chessdex/desktop tauri build",
    "build:all": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec -- rm -rf dist node_modules"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}